/*
#include "TJPosition.h"
#include "Aimer.h"
TJPosition::TJPosition()
{
	x = 0;
	y = 0;
	rightProx(0,1);
	leftProx(2,3);
	encoX();
	encoY();
	encoX.Reset();
	encoY.Reset();
}

void TJPosition::getLagPosition()
{
	/*
	if(Aimer::GetAge() < 1)
	{
		float pX =
	}
 }

void TJPosition::getProbablyPosition()
{
	getLagPosition();
	x += encoX.GetDistance();
	y += encoY.GetDistance();
	encoY.Reset();
	encoX.Reset();
	SmartDashboard::PutNumberArray("Positionator", new double[]{x,y});
}
*/
